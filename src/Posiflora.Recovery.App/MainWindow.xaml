<ui:FluentWindow x:Class="Posiflora.Recovery.App.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="POS Radar"
                 Height="720"
                 Width="1180"
                 MinHeight="620"
                 MinWidth="980"
                 WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="POS Radar" ShowMaximize="True" ShowMinimize="True" ShowClose="True" />

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="360" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Padding="16"
                    Background="#F7F8FA"
                    BorderBrush="#E6E8EC"
                    BorderThickness="1"
                    CornerRadius="8">
                <DockPanel LastChildFill="True">
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,20">
                        <TextBlock Text="Профили" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding StatusText}" Margin="0,8,0,0" Foreground="#616875" TextWrapping="Wrap" />
                    </StackPanel>

                    <ListBox ItemsSource="{Binding Profiles}"
                             SelectedItem="{Binding SelectedProfile}"
                             BorderThickness="0"
                             Background="Transparent">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,6">
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Description}" Foreground="#6B7280" TextWrapping="Wrap" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <Grid Grid.Column="1" Margin="18,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock Text="Findings" FontSize="24" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding SummaryText}" Foreground="#616875" />
                </StackPanel>

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding Findings}"
                         SelectedItem="{Binding SelectedFinding}"
                         BorderBrush="#E6E8EC"
                         BorderThickness="1"
                         Background="White">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="96" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0"
                                        Width="82"
                                        Height="30"
                                        CornerRadius="6"
                                        Background="{Binding SeverityBackground}">
                                    <TextBlock Text="{Binding Severity}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Foreground="White"
                                               FontWeight="SemiBold" />
                                </Border>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" />
                                    <TextBlock Text="{Binding Source}" Foreground="#6B7280" Margin="0,4,0,0" />
                                    <TextBlock Text="{Binding Evidence}" Foreground="#4B5563" TextWrapping="Wrap" Margin="0,8,0,0" />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>

            <Border Grid.Column="2"
                    Padding="18"
                    Background="#FFFFFF"
                    BorderBrush="#E6E8EC"
                    BorderThickness="1"
                    CornerRadius="8">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel DataContext="{Binding SelectedFinding}">
                        <TextBlock Text="Детали" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding Title}" Margin="0,14,0,0" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" />

                        <TextBlock Text="Evidence" Margin="0,22,0,6" FontWeight="SemiBold" Foreground="#374151" />
                        <TextBlock Text="{Binding Evidence}" TextWrapping="Wrap" Foreground="#4B5563" />

                        <TextBlock Text="Explanation" Margin="0,22,0,6" FontWeight="SemiBold" Foreground="#374151" />
                        <TextBlock Text="{Binding Explanation}" TextWrapping="Wrap" Foreground="#4B5563" />

                        <TextBlock Text="Recommended action" Margin="0,22,0,6" FontWeight="SemiBold" Foreground="#374151" />
                        <TextBlock Text="{Binding RecommendedAction}" TextWrapping="Wrap" Foreground="#4B5563" />

                        <CheckBox Content="Auto-fix available"
                                  IsChecked="{Binding CanAutoFix}"
                                  IsEnabled="False"
                                  Margin="0,22,0,0" />
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</ui:FluentWindow>
